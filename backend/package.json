{
  "name": "embedded-wallet-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "build:types": "wrangler types --include-runtime=false",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "prepreview": "pnpm build:types",
    "preview": "pnpm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.0.0",
    "hono": "^4",
    "pg": "^8.11.0",
    "porto": "latest",
    "viem": "^2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@cloudflare/workers-types": "^4.20250628.0",
    "@types/node": "^24.2.0",
    "@types/pg": "^8.11.0",
    "globals": "^16.0.0",
    "typescript": "~5.8.3",
    "wrangler": "^4.28.1"
  }
}