# JWT Secret for authentication
JWT_SECRET=foobarbaz

# Redis/KV Store URL for session storage
KV_URL=redis://localhost:6379

# Optional: AWS KMS Key ID for credential encryption
# If not provided, a new KMS key will be created when first setting up credentials
KMS_KEY_ID=your-kms-key-id-here

# Optional: PostgreSQL for testing (if not using user-provided DB)
DATABASE_URL=postgresql://localhost:5432/embedded_wallet

# Note: AWS credentials are now stored securely using KMS encryption
# Use the frontend credential setup or API endpoints to configure:
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY  
# - AWS_REGION
# - AWS_SECURITY_GROUP_ID
# - EC2_KEY_NAME
# - EC2_PRIVATE_KEY